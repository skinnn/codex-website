/**
 * App-wide display configuration.
 * Centralized so you can tweak values without hunting through pages.
 */

export const displayConfig = {
  /** Number of items shown initially in project/design/case-study grids */
  gridPageSize: 6,
}
